{
  "name": "defuddle-clipper-agent",
  "private": true,
  "version": "0.2.0",
  "workspaces": [
    "apps/extension",
    "apps/desktop/frontend"
  ],
  "scripts": {
    "build": "npm run build -w @dca/extension && npm run build -w frontend",
    "typecheck": "npm run typecheck -w @dca/extension && npm run check -w frontend",
    "clean": "npm run clean -w @dca/extension"
  }
}
